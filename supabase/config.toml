# Supabase CLI project config. Run `supabase link --project-ref <your-project-ref>`
# after creating a hosted project, then `supabase db push` to apply migrations
# and `supabase functions deploy` to ship the edge functions. See README.md.
project_id = "tongits"

[api]
enabled = true
port = 54321

[db]
port = 54322

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173"]

[realtime]
enabled = true
